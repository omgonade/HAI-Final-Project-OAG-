// Milestone 3 reuses Milestone 2's fairness dashboard unchanged — the
// group-level numbers are the same analysis, and duplicating the page would
// mean two copies to keep in sync. It is mounted under /m3 so the navigation
// stays inside one milestone as the user moves around.
export { default } from "@/app/m2/fairness/page";
